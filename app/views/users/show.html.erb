<h1> Welcome <%= current_user.name %> ! </h1>

<% current_user.classrooms.each do |class_room| %>
  <%= link_to "#", title: "link", class: "class center btn btn-lg btn-primary" do %>
    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> 
    <br>
    <br>
    <%= class_room.name %>
  <% end %>
<% end %>

